<Window x:Class="WhoIsTweeting.PinInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WhoIsTweeting"
        mc:Ignorable="d"
        Title="{StaticResource SignIn_Title}" Height="300" Width="300" SizeToContent="Height" WindowStartupLocation="CenterOwner" Icon="../wit.ico" ShowInTaskbar="False">
    <StackPanel Margin="5">
        <Label HorizontalAlignment="Center">
            <TextBlock TextAlignment="Center" Text="{StaticResource SignIn_Message}"/>
        </Label>
        <TextBox Text="{Binding PIN, Mode=TwoWay}" Margin="10,5" Padding="0,2" TextAlignment="Center" FontSize="16"/>
        <Button Content="{StaticResource Button_OK}" HorizontalAlignment="Center" Margin="0,5" Padding="15,3" Click="OnOKClicked"/>
    </StackPanel>
</Window>
