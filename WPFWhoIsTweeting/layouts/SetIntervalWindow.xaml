<Window x:Class="WhoIsTweeting.SetIntervalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WhoIsTweeting"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance {x:Type local:IntervalViewModel}}"
        Title="{x:Static local:Strings.Interval_Title}" Height="246" Width="340" ResizeMode="NoResize" SizeToContent="Height" ShowInTaskbar="False" WindowStartupLocation="CenterOwner" Icon="../wit.ico">
    <StackPanel Margin="5">
        <Label>
            <Label.Content>
                <TextBlock>
                    <InlineUIContainer>
                		<TextBlock Text="{x:Static local:Strings.Interval_Message}"/>
                	</InlineUIContainer>
                </TextBlock>
            </Label.Content>
        </Label>
        <StackPanel HorizontalAlignment="Stretch" Background="{x:Static SystemColors.ControlBrush}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Label Content="{Binding Interval}" ContentStringFormat="{x:Static local:Strings.Interval_SecondFormat}" FontSize="16"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="FontFamily" Value="Segoe UI Symbol"/>
                        <Setter Property="FontSize" Value="16"/>
                        <Setter Property="Padding" Value="3,0,3,0"/>
                    </Style>
                </StackPanel.Resources>
                <Button Content="&#xe0e5;" BorderThickness="1,1,0,1" Click="OnDownClicked"/>
                <Button Content="&#xe0e4;" Click="OnUpClicked"/>
            </StackPanel>
            <Label HorizontalAlignment="Center" Content="{Binding MaxFollowings}" ContentStringFormat="{x:Static local:Strings.Interval_MaxFollowingsFormat}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Padding" Value="15,3,15,3"/>
                    <Setter Property="Margin" Value="5"/>
                </Style>
            </StackPanel.Resources>
            <Button Content="{x:Static local:Strings.Button_OK}" Click="OnOKClicked"/>
            <Button Content="{x:Static local:Strings.Button_Cancel}" Click="OnCancelClicked"/>
        </StackPanel>
    </StackPanel>
</Window>
